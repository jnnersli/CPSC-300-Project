function toggleText(button_id) {
	if(document.getElementById('button_id').textContent == "Lock") {
		document.getElementById('button_id').textContent = "Unlock";
	} else {
		document.getElementById('button_id').text="Lock"
	}
}
